import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export function VideoBackground() {
  return (
    <Box>
      <Typography variant="body2" color="text.secondary">
        VideoBackground placeholder
      </Typography>
    </Box>
  );
}
